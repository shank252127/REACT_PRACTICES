// npm is not short of node package manager. Its everything but not node package manager, its doesnt have fullform;
// standard reporsitory of all the packages, or say library, its an package manager;

// npm init to create package.json;

// npm i -D parcel, parcel is a bundler which combines all the files (css,html,js) together. It bundles ur app so that it can be shipped to production;
// -D above stands for the dev dependencies
// React bydefault uses wepack bundler;

// There are two type of dependencies -  1- dev depencencies; 2- Normal Dependencies

// "^2.13.3" -